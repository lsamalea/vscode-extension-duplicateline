{
	"name": "duplicateline",
	"displayName": "duplicateline",
	"description": "duplicate a line",
	"version": "0.0.1",
	"publisher": "lsamalea",
	"engines": {
		"vscode": "^0.10.1"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.duplicateline"
	],
	"main": "./out/src/extension",
	"contributes": {
		"commands": [{
			"command": "extension.duplicateline",
			"title": "duplicate line"
		}]
	},
	"scripts": {
		"vscode:prepublish": "node ./node_modules/vscode/bin/compile",
		"compile": "node ./node_modules/vscode/bin/compile -watch -p ./"
	},
	"devDependencies": {
		"typescript": "^1.6.2",
		"vscode": "0.10.x"
	}
}